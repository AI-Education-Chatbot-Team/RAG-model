# RAG-model
This is a sample RAG model for the AI Education Chatbot Team



## Requirements
To install dependencies from package.json run  `npm install`

Node v24.21.0
`https://nodejs.org/en/download`

Typescript
`npm install typescript @types/node --save-dev`

## How to run 
`node src/index.ts`